DATABASE_URL="postgresql+asyncpg://user:password@localhost:5432/motospecs"
API_NINJAS_KEY="your-api-ninjas-key"
API_NINJAS_URL="https://api.api-ninjas.com/v1/motorcycles"
VITE_URL_BASE_API_DEV="http://localhost:5173"
WIKI_USER_AGENT="throttleIQ (https://github.com/BasicallyManny/ThrottleIQ)"

# Comma-separated list of allowed CORS origins for production, e.g.
# ALLOWED_ORIGINS="https://throttleiq.app,https://www.throttleiq.app"
# Falls back to VITE_URL_BASE_API_DEV when unset.
ALLOWED_ORIGINS=""

# Set to "true" to log every SQL statement (noisy — leave off in production).
SQL_ECHO="false"
